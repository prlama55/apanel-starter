import { Typography, Box } from '@strapi/design-system';

export const ChatHeader: React.FC = () => {
  return (
    <Box marginBottom={2}>
      <Typography variant="beta">AI Assistant</Typography>
    </Box>
  );
};
